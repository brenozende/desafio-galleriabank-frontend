services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: desafio-galleriabank-front
    ports:
      - "4200:4200"
    environment:
      - CHOKIDAR_USEPOLLING=true
    stdin_open: true
    tty: true
